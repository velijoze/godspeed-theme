<!DOCTYPE html>
<!--[if IE 8]><html class="no-js lt-ie9" lang="en"> <![endif]-->
<!--[if IE 9 ]><html class="ie9 no-js"> <![endif]-->
<!--[if (gt IE 9)|!(IE)]><!--> <html class="no-js p-0" {% if settings.enable_rtl_mode %} dir="rtl"{% endif %} > <!--<![endif]-->
<head>
  <!-- Basic page needs ================================================== -->
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">

  <!-- Title and description ================================================== -->
  <title>
    {{ page_title }}{% unless page_title contains shop.name %} &ndash; {{ shop.name }}{% endunless %}
  </title>

  {% if page_description %}
  <meta name="description" content="{{ page_description | escape }}">
  {% endif %}

  <!-- Helpers ================================================== -->
  {% include 'social-meta-tags' %}
  <link rel="canonical" href="{{ canonical_url }}">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="theme-color" content="{{ settings.m_primary_color }}">
  <link rel="icon" href="{{'favicon.png'| asset_url}}" type="image/png" sizes="16x16">

  <!-- Resource hints for performance -->
  <link rel="preconnect" href="https://fonts.gstatic.com">
  <link rel="preconnect" href="https://cdn.shopify.com">
  
  <!-- Font includes -->
  {% include 'engo-header-fonts' %}
  
  <!-- Critical CSS -->
  <style>
    :root {
      --font-body: {% if settings.body_font %}{{ settings.body_font.family }}, {{ settings.body_font.fallback_families }}{% else %}inherit{% endif %};
      --font-heading: {% if settings.heading_font %}{{ settings.heading_font.family }}, {{ settings.heading_font.fallback_families }}{% else %}inherit{% endif %};
      --font-nav: {% if settings.navigation_font %}{{ settings.navigation_font.family }}, {{ settings.navigation_font.fallback_families }}{% else %}inherit{% endif %};
    }
    *,*::before,*::after{box-sizing:border-box}
    body{margin:0;font-family:var(--font-body);line-height:1.5;color:#333;background-color:#fff}
    img{max-width:100%;height:auto;display:block}
    .password-page{min-height:100vh;display:flex;align-items:center;justify-content:center;padding:20px}
    .password-container{max-width:500px;width:100%;text-align:center;padding:40px;box-shadow:0 4px 6px rgba(0,0,0,0.1);border-radius:8px;background:#fff}
    .logo{display:inline-block;font-size:24px;font-weight:600;margin-bottom:30px;font-family:var(--font-heading)}
    h1,h2,h3,h4,h5,h6{margin-top:0;margin-bottom:0.5rem;font-weight:500;line-height:1.2;font-family:var(--font-heading)}
    h1{font-size:2.5rem}h2{font-size:2rem}p{margin-top:0;margin-bottom:1rem}
    .btn{display:inline-block;padding:0.75rem 1.5rem;font-size:1rem;line-height:1.5;text-align:center;text-decoration:none;border:1px solid transparent;transition:all 0.3s ease;cursor:pointer}
    .btn-primary{color:#fff;background-color:#007bff;border-color:#007bff}
    .form-group{margin-bottom:1rem;text-align:left}
    .form-control{display:block;width:100%;padding:0.75rem;font-size:1rem;line-height:1.5;color:#495057;background-color:#fff;background-clip:padding-box;border:1px solid #ced4da;border-radius:0.25rem;transition:border-color 0.15s ease-in-out,box-shadow 0.15s ease-in-out;font-family:var(--font-body)}
    .form-control:focus{color:#495057;background-color:#fff;border-color:#80bdff;outline:0;box-shadow:0 0 0 0.2rem rgba(0,123,255,0.25)}
    label{display:inline-block;margin-bottom:0.5rem;font-weight:500;font-family:var(--font-body)}
  </style>

  <!-- CSS ================================================== -->
  {% if settings.po_style == "po_swatchs" %}
  {{ 'timber.scss.css' | asset_url | stylesheet_tag }}
  {% endif %}
  {% if settings.po_style == "po_dropdown" %}
  {{ 'timber_2.scss.css' | asset_url | stylesheet_tag }}
  {% endif %}

  {{ 'engo-customizes.css' | asset_url | stylesheet_tag }}

  <!-- Header hook for apps ================================================== -->
  {{ content_for_header }}
</head>

<body class="template-password password-page">
  <div class="password-page">
    <div class="password-container">
      {% if shop.logo %}
        <div class="logo">
          <img src="{{ shop.logo | img_url: '200x' }}" alt="{{ shop.name }}" />
        </div>
      {% else %}
        <div class="logo">{{ shop.name }}</div>
      {% endif %}

      {{ content_for_layout }}
    </div>
  </div>

  <!-- JS ================================================== -->
  {{ 'modernizr-3.13.1.min.js' | asset_url | script_tag }}
  {{ 'jquery-3.7.1.min.js' | asset_url | script_tag }}
  {{ 'timber.js' | asset_url | script_tag }}
</body>
</html>
